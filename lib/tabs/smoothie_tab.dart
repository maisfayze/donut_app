import 'package:flutter/cupertino.dart';

class Smooth extends StatelessWidget {
  const Smooth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Smooth'),
      ),
    );
  }
}
